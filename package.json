{
  "name": "@voderl/har-cli",
  "version": "0.1.1",
  "description": "Agent-friendly CLI for parsing, querying, and filtering HAR files.",
  "bin": {
    "har-cli": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/cli.ts",
    "start": "node dist/cli.js",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
